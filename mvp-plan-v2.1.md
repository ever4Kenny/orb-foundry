# 《弹珠迷宫：Orb Foundry》MVP 方案 v2.1

版本：v2.1
日期：2026-05-17
定位：在 v2 MVP 基础物理验证通过后，注入 roguelike 选择密度。MVP+ 阶段。

---

## 一、相对 v2 的变更摘要

v2 验证已通过：物理可玩、球感差异可感知。当前痛点：**roguelike 元素不足，玩家局内选择密度太低，relic 系统缺位，盘面改造池太小**。v2.1 在不破坏"0 职业、0 敌人"原则的前提下，注入选择密度。

| 维度 | v2 | v2.1 | 理由 |
|---|---|---|---|
| 起始 relic | 无 | 三选一 | 起手即有构筑感 |
| Relic 池 | 无 | 6 个永久被动 | 给中后段提供差异化 |
| 第 1 关后 | 球三选一 | 球三选一（文案改为"扩充球袋"）| 避免与 relic 混淆 |
| 第 2 关后 | 盘面改造二选一 | relic 三选一 + 盘面改造二选一 | 选择密度翻倍 |
| 改造池 | 2 个 | 6 个 | 二选一无放回，局内体验差异 |
| 主动技能 | 无 | 1 个固定技能"重投"，每局 2 次 | 让玩家拥有局内主动权 |
| 关前玩法说明 | 每关弹窗确认 | 删除 | Combo 已变为全局机制，无需逐关教学 |
| 技术栈 | Cocos Creator | **Godot 4.6** | 已迁移，团队实际开发栈 |
| 目标分口径 | 600/1200/2000（累计） | **单关 600/800/1000，累计 600/1400/2400** | 修正口径误差，单关目标更易读 |

**核心不变：1 盘 / 4 球 / 3 轮 / 0 敌人 / 0 职业 / 0 商店。**

---

## 二、MVP+ 验证目标

只回答 2 个新问题（v2 的 3 个已通过）：

| # | 问题 | 通过标准 |
|---|---|---|
| 1 | 引入 relic 后，玩家是否产生"这局起手不亏"的构筑感？ | 内部试玩中 ≥3/5 玩家在第 2 关后能说出自己当前的"流派" |
| 2 | 选择密度从 2 次 → 7 次后，是否仍可在 5 分钟内打完一局？ | 单局完整时长 3~6 分钟 |

---

## 三、局内流程

```
开局
  ↓
[选择] Relic 三选一  ← 新增
  ↓
第 1 关（单关目标 600 分 / 累计 600，发球 4 颗）
  ↓
[选择] 球三选一（扩充球袋）+ 盘面改造二选一
  ↓
第 2 关（单关目标 800 分 / 累计 1400，发球 5 颗）
  ↓
[选择] Relic 三选一 + 盘面改造二选一  ← 新增
  ↓
第 3 关（单关目标 1000 分 / 累计 2400，发球 5 颗）= 终关
  ↓
结算
```

**局内玩家选择次数：1（起始 relic）+ 1（球）+ 1（改造）+ 1（relic）+ 1（改造）= 5 次主动选择，加上每轮"重投"主动技能（2 次充能）= 最多 7 次有意义选择。**

---

## 四、Relic 系统

### 4.1 抽取规则

- 6 个 relic 候选池
- 开局 + 第 2 关后共 2 次三选一，无放回（同一 relic 不会出现两次）
- 全部为永久被动效果，全局生效到本局结束

### 4.2 Relic 池（6 个）

| ID | 名 | 效果 | 设计意图 |
|---|---|---|---|
| R1 | 熔炉残温 | 每关开始时盘面随机移除 1 红 peg + 1 灰 peg | 永久减负，对应"危险清除"长期化 |
| R2 | 奖励共振 | 击中任意奖励 peg 时额外 +15 分 | 滚雪球向，强化奖励 peg 流派 |
| R3 | 额外弹仓 | 每轮抽球数 3 → 4（仍选 1 颗发射） | 选择密度 +33%，强化球袋系统 |
| R4 | 裂纹累积 | 玻璃球分裂阈值 6 → 4 次碰撞 | 球种特化向 |
| R5 | 重锤校准 | 中槽得分 +10 → +25 | 改变最优瞄准（中下→精准落槽） |
| R6 | 磁极强化 | 磁球吸附范围 ×1.5 | 球种特化向 |

**覆盖维度：盘面 / 分数 / 选择 / 球效果 ×2 / 槽位。**

### 4.3 配置 schema

```json
{
  "relics": [
    {
      "id": "R1",
      "name": "熔炉残温",
      "desc": "每关开始时盘面随机移除 1 红 peg + 1 灰 peg",
      "trigger": "onRoundStart",
      "effect": {"type": "remove_pegs", "by_type": {"danger": 1, "normal": 1}, "mode": "random"}
    },
    {
      "id": "R2",
      "name": "奖励共振",
      "desc": "击中奖励 peg 额外 +15 分",
      "trigger": "onPegHit",
      "tag_filter": ["bonus"],
      "effect": {"type": "score_bonus", "value": 15}
    }
  ]
}
```

触发器使用 v2 已预留的 `onPegHit(peg, ball)` / 新增 `onRoundStart()` / `onBallLaunch()` 等钩子。

---

## 五、起始主动技能

```
名称：重投
效果：丢弃当前 3 颗候选球，从球袋重新抽 3 颗
充能：每局 2 次
触发：玩家点击发球区下方的"重投"按钮
UI：按钮显示剩余次数（2 → 1 → 0 灰显）
不消耗：球袋中的球（重投是从同一球袋里再抽，不补充新球）
```

**理由：让玩家在"运气不好抽到不适配当前盘面的球"时有破局手段，强化球袋作为系统的存在感。**

---

## 六、盘面改造池

### 6.1 抽取规则

- 6 个改造候选池
- 第 1 关后 + 第 2 关后共 2 次二选一，无放回
- 立即生效，影响后续所有关卡

### 6.2 改造池（6 个）

| ID | 名 | 效果 | 适配流派 |
|---|---|---|---|
| B1 | 金属镀层 | 盘面中部新增 4 个奖励 peg | 磁球、玻璃球 |
| B2 | 危险清除 | 移除所有危险 peg（同时移除等量普通 peg） | 铁球、水晶球 |
| B3 | 中心漏斗 | 盘面中下部新增 3 排奖励 peg 形成漏斗形 | R5 重锤校准 |
| B4 | 双子通道 | 中部开一条直通中槽的空通道（移除约 6 个 peg） | 想稳落槽的玩家 |
| B5 | 镜面边墙 | 左右边缘加反弹墙，球更难脱离盘面 | 水晶球（高弹跳） |
| B6 | 血色盘面 | 危险 peg ×2，但全部击中后清空盘面 +50 分 | 高风险铁球流 |

**覆盖维度：加奖励 / 减危险 / 改地形 ×2 / 改物理边界 / 高风险高回报。**

### 6.3 配置 schema

```json
{
  "board_upgrades": [
    {"id": "B1", "name": "金属镀层", "effect": "add_pegs", "peg_type": "bonus", "count": 4, "region": "center"},
    {"id": "B2", "name": "危险清除", "effect": "remove_pegs", "peg_type": "danger", "also_remove": "normal", "ratio": "1:1"},
    {"id": "B3", "name": "中心漏斗", "effect": "add_pegs_pattern", "pattern": "funnel", "peg_type": "bonus", "count": 9, "region": "center_bottom"},
    {"id": "B4", "name": "双子通道", "effect": "remove_pegs_pattern", "pattern": "vertical_corridor", "count": 6, "region": "center"},
    {"id": "B5", "name": "镜面边墙", "effect": "add_wall", "position": "edges", "reflectivity": 1.0},
    {"id": "B6", "name": "血色盘面", "effect": "multi", "operations": [
      {"op": "multiply_peg_count", "peg_type": "danger", "factor": 2},
      {"op": "register_trigger", "trigger": "onAllDangerCleared", "effect": {"type": "score_bonus", "value": 50}}
    ]}
  ]
}
```

---

## 七、Combo 机制（每轮通用）

**v2.1 修正：Combo 是每一关都生效的全局机制，不再每关弹窗教学。**

- 同一颗球连续命中 peg 不落槽时，每次命中分数 ×(1 + 0.1 × combo_count)
- combo 上限 10 倍（即第 10 次命中开始锁定 ×2.0）
- 落槽或球停止时重置
- UI：球飞行过程中右上角显示当前 combo 倍率（实时）

**关前弹窗删除，第 1 关开始前用一个 2 秒的 toast 简单提示一次：「连续命中可叠加倍率」，之后不再出现。**

---

## 八、其余系统（沿用 v2）

以下沿用 v2 设定，不展开：

- 盘面：竖屏 9:16，8 行 × 3~5 列，普通/奖励/危险 三种 peg，比例 70/15/15
- 球种：铁 / 水晶 / 磁 / 玻璃 四种，效果穿透 / 高弹跳 / 吸附 / 分裂
- 槽位：左（弹性+10%）/ 中（+10 分，受 R5 影响变 +25）/ 右（回复 1 颗球）
- 球袋：初始 6 颗，每轮抽 3（受 R3 影响变 4），用过不放回
- 关卡：3 关，单关目标分 600/800/1000（累计 600/1400/2400），发球数 4/5/5
- 数据结构：peg / ball / relic 全部带 `tags` 字段，触发器系统钩子已预留

---

## 九、技术栈（v2.1 修正）

**v2 写的 Cocos Creator 3.x 已废弃**，团队实际开发栈为 **Godot 4.6**。

- 引擎：Godot 4.6 stable（Linux 版本已在 `~/orb_foundry/godot/` 工作）
- 物理：Godot 内置 2D 物理（`PhysicsBody2D` + `RigidBody2D`）
- 配置：本地 JSON / Godot `Resource` 文件
- 平台：桌面（Linux/Windows）原型，浏览器导出为可选
- 项目结构：`scenes/` / `scripts/` / `resources/` / `addons/`
- 已有产物：CODEX_TASK*.md、godot-mvp-validation 等迁移文档

### 9.1 v2.1 新增工程任务

| 模块 | 工期估计 | 负责 |
|---|---|---|
| Relic 系统（触发器框架 + 6 个效果实现） | 2 天 | tech 小码 |
| 改造池扩展（B3~B6 新逻辑） | 1.5 天 | tech 小码 |
| 起始 relic 三选一 UI | 0.5 天 | tech + art |
| 第 2 关后 relic 三选一 UI | 0.5 天 | tech + art |
| "重投"按钮 + 球袋逻辑 | 0.5 天 | tech 小码 |
| 删除关前弹窗 + 加 toast | 0.5 天 | tech 小码 |
| Combo UI 实时显示（右上角） | 0.5 天 | tech + art |
| 配置 JSON schema 升级 | 0.5 天 | tech 小码 |
| 自测 + 录屏 | 1 天 | gameplay 小玩 |

**总计：约 7~8 个工作日。**

### 9.2 架构对齐（沿用 v2）

- 触发器系统：`onPegHit` / `onRoundStart` / `onBallLaunch` / `onAllDangerCleared` 等钩子
- BoardModifier 模块：盘面改造独立成可叠加的 effect
- RelicManager 模块：管理已激活 relic 列表，调度触发器

### 9.3 不做

- 不做 relic 稀有度 / 颜色 / 商店
- 不做主动 relic（除"重投"外全部被动）
- 不做 relic 升级 / 合成
- 不做敌人 / Boss
- 不做稳定度 / 轨迹预览
- 不做职业系统
- 不做局外成长 / 解锁

---

## 十、美术边界

沿用 v2：纯几何形状 + 颜色区分。零美术资产。

**v2.1 新增表现需求（仍用几何形状）：**

| 元素 | 表现 |
|---|---|
| Relic 选择卡片 | 矩形卡片 + 标题文字 + 描述文字，3 张并列 |
| Relic 已激活栏 | 顶部一排小图标（用 emoji 或几何占位） |
| 重投按钮 | 圆形按钮 + 剩余次数数字 |
| Combo 倍率 | 右上角数字 "×1.5"，红→金渐变高亮 |
| 改造选择卡片 | 同 relic 卡片样式，2 张并列 |

**美术分工：小美做卡片排版 + 颜色，不做图标插画。**

---

## 十一、验收标准

| 指标 | 最低标准 | 验证方式 |
|---|---|---|
| v2 验收项全部保留 | 见 v2 第八章 | 回归测试 |
| Relic 系统可用 | 6 个 relic 全部能正常触发 + 显示在已激活栏 | 自测每个 relic 单独录屏 |
| 改造池可用 | 6 个改造全部能正常应用到盘面 | 自测每个改造单独录屏 |
| 重投按钮可用 | 点击后 3 颗候选球被替换，次数 -1 | 自测 |
| Combo 显示正常 | 连续命中时倍率实时上升，落槽后重置 | 自测 |
| 单局时长 | 3~6 分钟 | 自测 + 内部试玩 |
| 内部试玩"构筑感" | 5 人中 ≥3 人能在第 2 关后说出自己流派 | 定性访谈 |
| 内部试玩"耐玩度" | 5 人中 ≥3 人愿意再开一局 | 定性访谈 |

---

## 十二、通过 / 失败裁决

### 通过条件（全部满足）

1. v2 所有验收项保持通过
2. Relic 三选一 + 改造二选一 + 球三选一 全部流程顺畅
3. 玩家在第 2 关后能说出自己的构筑流派
4. 单局时长可控在 6 分钟内
5. 内部试玩 ≥3/5 愿意再开

### 通过 → 下一层（v2.2 候选方向）

- 加入第 1 种敌人（护盾 peg）
- 加入 1 个 Boss 关
- 加入稳定度资源
- Relic 池扩展到 12 个
- 引入主动 relic 概念

### 失败 → 回退

不增加更多选择密度。回到 v2 物理优化方向。

---

## 十三、与 v2 的差异表（详版）

| 变更 | v2 | v2.1 | 理由 |
|---|---|---|---|
| Relic | 无 | 6 个永久被动 + 2 次三选一 | 注入 roguelike 构筑维度 |
| 主动技能 | 无 | "重投"每局 2 次 | 给玩家局内主动权 |
| 改造池 | 2 个 | 6 个 | 二选一无放回，体验差异化 |
| 第 1 关后 | 球三选一 | 球三选一（文案"扩充球袋"） + 改造二选一 | 选择密度提升 |
| 第 2 关后 | 改造二选一 | relic 三选一 + 改造二选一 | 选择密度提升 |
| Combo | 每轮弹窗教学 | 全局机制，仅首关 2 秒 toast | 消除冗余教学 |
| 触发器钩子 | 仅 `onPegHit` 留空 | `onPegHit` / `onRoundStart` / `onBallLaunch` / `onAllDangerCleared` | 支撑 relic + 改造 |
| 数据结构 | peg/ball 有 tags | + relic 有 tags + trigger | 触发器统一 |
| 技术栈 | Cocos Creator 3.x | **Godot 4.6** | 团队实际栈，v2 写错 |

---

## 十四、附：球袋文案修正

| 场景 | v2 文案 | v2.1 文案 |
|---|---|---|
| 第 1 关后选择 | "选择 1 颗新球" | "**扩充球袋**：选 1 颗新球加入你的弹仓（永久）" |
| 选项卡片 | 球名 + 简短效果 | 球名 + 简短效果 + "加入球袋后每轮抽球时可能抽到" 提示一行 |

**目的：让玩家明确这不是 relic、不是一次性消耗，而是球袋永久扩充。**
